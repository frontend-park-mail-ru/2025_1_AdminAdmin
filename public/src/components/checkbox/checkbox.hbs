<div class="checkbox-container">
    <input
            type="checkbox"
            id="{{id}}"
        {{#if checked}}checked{{/if}}
        {{#if disabled}}disabled{{/if}}
    />
    <label for="{{id}}">{{label}}</label>
</div>
