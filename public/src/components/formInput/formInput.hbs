<div class="form__input" id="{{id}}">
    <div class="form__input-head">
        <span>{{label}}</span>
        <div class="form__input__error" id="{{id}}-error"></div>
    </div>
    <input type="{{type}}" placeholder="{{placeholder}}" {{#if required}}required{{/if}}>
</div>
