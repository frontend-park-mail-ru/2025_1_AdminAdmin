<div class="promocode-card" id="{{id}}">
    <span class="promocode-card__promocode-text">{{promocodeText}}</span>
    <span class="promocode-card__discount-size">Скидка {{discountSize}}%</span>
    <div class="promocode-card__copy-button-container"></div>
    {{#if expiresAt}}
    <span class="promocode-card__expires-at_desktop">Действителен до: {{expiresAt}}</span>
    <span class="promocode-card__expires-at_mobile">До: {{expiresAt}}</span>
    {{/if}}
</div>